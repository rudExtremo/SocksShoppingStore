# Дорожная карта

Этот документ объединяет и актуализирует пункты из исторических `.codex` RTF/PDF.

## Сейчас

- Закрепить структуру документации: EN по умолчанию + RU‑версии
- Проверить security headers и CSP для всех вью и ассетов
- Убедиться, что дефолтные лимиты безопасны для демо

## Далее

- Добавить unit‑тесты для `CartController` и `ProductCatalogLocalizer`
- Расширить каталог товаров и RU‑локализацию
- Опционально: простая персистентность (например, LiteDB) за интерфейсом

## Потом

- Интегрировать аутентификацию и профили (опционально)
- CI‑проверки (format, lint, базовые тесты) и контейнеризация
- Dark mode и небольшой UI‑полиш

